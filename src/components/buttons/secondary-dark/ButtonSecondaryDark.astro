---
export interface Props {
  href?: string;
  target?: string;
  disabled?: boolean;
}

const { 
  href = '#',
  target = '_self',
  disabled = false
} = Astro.props;
---

<a 
  href={href} 
  target={target}
  class={`btn btn-secondary-dark ${disabled ? 'disabled' : ''}`}
  aria-disabled={disabled}
>
  <slot />
</a>

<style>
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-sans);
    font-weight: 500;
    text-decoration: none;
    border-radius: 0px;
    transition: all 0.3s ease;
    cursor: pointer;
    border: 2px solid;
    position: relative;
    overflow: hidden;
  }

  .btn:focus {
    outline: none;
    box-shadow: 0 0 0 3px rgba(255, 255, 255, 0.1);
  }

  .btn.disabled {
    opacity: 0.5;
    cursor: not-allowed;
    pointer-events: none;
  }

  .btn-secondary-dark {
    gap: 6px;
    background-color: rgba(255, 255, 255, 0.1);
    color: var(--color-white);
    border-color: rgba(255, 255, 255, 0.3);
    padding: 10px 20px;
    font-size: 14px;
    font-weight: 500;
    min-width: 120px;
    line-height: 1.2;
  }

  .btn-secondary-dark:hover {
    background-color: rgba(255, 255, 255, 0.2);
    color: var(--color-white);
    text-decoration: none;
    transform: translateY(-1px);
  }

  .btn-secondary-dark:active {
    transform: translateY(0);
  }
</style>
